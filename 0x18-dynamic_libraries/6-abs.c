#include "main.h"


/**
 *_abs- get the absolute number of integer
 *@b: absolute computed
 *Return: 0
 */

int _abs(int b)
{
	int c;

	if (b < 0)
	{
		int a = -1;

		c = a * b;
		_putchar(c);
	}
	return (c);
}
