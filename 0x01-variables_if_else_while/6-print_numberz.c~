#include<stdio.h>
/**
 *main - this function print ascii of based 10 using putchar
 *Return: return 0 if succeed
 */

int main(void)
{
	int num = '0';
	while (num <= '9')
	{
		putchar(num);
		num++;
	}
	putchar('\n');
	return(0);
}
